
// Document ready
$(document).ready(function() {

    // Does the side nav exists
    if(!($('.sidenav').length === 0)) {
        // Init side nav
        $('.sidenav').sidenav();
    };
    //next code
});
